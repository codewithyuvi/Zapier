apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort # This allows us to access the UI from your browser!
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000